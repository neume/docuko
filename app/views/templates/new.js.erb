<% options = {
  url: [current_office, @data_model, :templates],
  template: @template,
  remote: true
} %>

modal_div = document.getElementById('app-modal')

modal_div.innerHTML = "<%= j render 'form_modal', options%>"
var app_modal = new bootstrap.Modal(document.getElementById('app-modal'))

app_modal.show()
